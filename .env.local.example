# 認証システム環境変数設定例
# このファイルを .env.local にコピーして設定値を入力してください

# ===== 基本設定 =====
NODE_ENV=development

# ===== データベース設定 =====
DATABASE_URL="file:./dev.db"

# ===== JWT設定 =====
# 32文字以上のランダムな秘密鍵を生成してください
# 生成例: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secure-jwt-secret-at-least-32-characters-long

# JWT有効期限（デフォルト: 48h）
# 形式: 数値 + 単位 (h=時間, d=日, m=分)
JWT_EXPIRES_IN=48h

# ===== LINE認証設定 =====
# LINE Developers Console から取得
LINE_CHANNEL_ID=1234567890
LINE_CHANNEL_SECRET=abcdef1234567890abcdef1234567890

# ===== Next.js認証設定 =====
# 開発環境
NEXTAUTH_URL=http://localhost:3000

# 本番環境例（実際のドメインに変更）
# NEXTAUTH_URL=https://your-app.pages.dev

# ===== 招待システム設定 =====
# 招待URLのデフォルト有効期限（デフォルト: 168h = 7日）
INVITE_DEFAULT_EXPIRES=168h

# ===== 設定ガイド =====
# 
# 1. JWT_SECRET の生成:
#    node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
# 2. LINE認証設定:
#    - https://developers.line.biz/console/ でチャンネル作成
#    - Callback URL: http://localhost:3000/api/auth/line/callback (開発環境)
#    - Callback URL: https://your-domain.com/api/auth/line/callback (本番環境)
#
# 3. 本番環境:
#    - NODE_ENV=production
#    - DATABASE_URL にはCloudflare D1のURL
#    - NEXTAUTH_URL には実際のドメイン
#    - JWT_SECRET は64文字以上推奨