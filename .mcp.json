{
  "mcpServers": {
    "container-use": {
      "command": "container-use",
      "args": ["stdio"]
    },
    "github": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "--env-file",
        ".env",
        "ghcr.io/github/github-mcp-server"
      ]
    }
  }
}
